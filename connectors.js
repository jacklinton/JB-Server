import Mongoose from 'mongoose';

const mongo = Mongoose.connect('mongodb://localhost/data/db');

const SongSchema = Mongoose.schema({

});

const ShowShema = Mongoose.schema({

});

const ArtistSchema = Mongoose.schema({

});
